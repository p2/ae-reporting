<% for (var i = 0; i < rules.length; i++) {
	var rule = rules[i]; %>

<div class="rule">
	<button onclick="_runRule(this, '<%= rule.id %>')">Check</button>
	<h2><%= rule.name %></h2>
	<p><%= rule.description %></p>
	<div class="rule_output"></div>
</div>

<% } %>
